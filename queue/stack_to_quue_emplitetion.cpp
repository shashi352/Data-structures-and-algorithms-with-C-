#include "bits/stdc++.h"
using namespace std;

class que{
	stack<int> st1;
	stack<int> st2;
	public:
		void push(int x);{
		st1.push(x);}
		
		void pop(int x){
        if(st2=!empty & st1=!empty )
		 s2.pop(x);
		}
		if(!empty=1st && !empty=2st){
			-1
		}
		
};

int main(){
	que q ;
	q.push();
}
