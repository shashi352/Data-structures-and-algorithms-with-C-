 #include<bits/stdc++.h>
 
 class node{
 	public:
 		int data ;
 		node* next;
 		
 		node(int val){
 		 	data= val;
 			next=NULL;
		 }

void insertathead(nohde* head&,, int val){
	node* new = n node(val)
	node*temp = head 
	while(temp!=head){
	head =head->next	
	n=head
}
while(NULL){
	node* new = n node(val)
    head=n
    NULL=n
}
}		 
		 
		 
void insertattail(node* head&, int val){
	node* n =new node(val)
	node* temp=head
	
	while(temp->next!=head){
		temp=temp->next
		
	}
	while(head== NULL){
		insertathead(head,val)
	}
	temp->next=n
	n-next=head
}

		 
void insertathead(node* head&, int val){
	if(head=NULL)
	n->next=n
	head=n
	return
}

node* n= new (node);
while(ttemp->next!=head){
temp->temp=next;

}
temp->n
n->>next=head;




temp ->next=n;
n->next=head



void display(){
	node* temp=head; 
	do{
		cout<<temp->data<<" ";
		temp=temp->next
	   
	}while(temp!=head);
	cout<<end;
}
int maim(){
	node* head=NULL;
	
	
	insertathead(head,1)
	insertathead(head,1)
	insertathead(head,1)
	insertathead(head,1)
	insertathead(head,1)
	insertathead(head,1)
}		 
 };
 
