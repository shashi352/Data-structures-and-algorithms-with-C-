#include<bits/std.h>
using namespace
class node {
	int data;
	node* next;
	
  node*(int val){
		data= val;
		next =NULL
	}
};

if(head==NULL){
	n->next=n;
	head= n;
	return
}

void insertathead(node* &head , int val) {
	node* n= node(val)
	while(temp->next=head){
	temp=temp->next;
}
	temp->next=n
	n->next=head;
	head=n;
}


void insertattil(node* &head,int val){
	
	if(head=NULL){
		insertathead(head , val)
	}
	
node* n = new node(val)

node* temp = head;

while(temp->next=head){
	temp=temp->next;
}
temp->next=n
n->next=head

void display(node* head)

node*temp=head{
do{
	cot<<temp->data<<" "
	temp=temp->next
}while(temp!head)

cout<<end;
int main(){

cout<<"hello  word"}
