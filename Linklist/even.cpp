#include<bits/stdc++.h>
using namespace

void disply(nod* head)
node* temmmp = head;
while (temp!=NULL))
cout<<temp->data;
}cout<<"NULL"<<end
}

void afterputodd()(odd!=NULL && even=NULL){
 node*  odd = head
 node*    head->next =  even
 node*   even=     evenstart
     	
	while{
	}
node* odd=even->
odd = odd->next
node* even= odd->
even=	even->next
	
}
odd->next=evenstart)
if(odd->next=NULL
even->next=NULL)

int main(){
	node* head;
	int arr[]=[123456];
	for(int i=o;<6;i++){
	}

for
}
